'use strict';

/**
 * newsletter-category router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::newsletter-category.newsletter-category');
